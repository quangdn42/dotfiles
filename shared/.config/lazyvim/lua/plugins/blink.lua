return {
  {
    "saghen/blink.cmp",
    optional = true,
    opts = {
      keymap = {
        preset = "default",
        ["<CR>"] = {},
      },
      -- Disabled in favor of noice.nvim signature support
      -- signature = { enabled = true },
    },
  },
}
